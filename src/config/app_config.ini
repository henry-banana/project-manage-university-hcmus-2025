[Database]
DataSourceType = SQL  ; Options: MOCK, SQL (CSV sẽ cần thêm logic)
SqlConnectionString = database/university.db ; Đường dẫn file SQLite

[Logging]
LogLevel = INFO      ; Options: DEBUG, INFO, WARN, ERROR, CRITICAL
LogFilePath = logs/app.log